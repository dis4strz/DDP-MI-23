#include <iostream>

using namespace std;

int main()
{
    int N;
    cin >> N;

    if (N >= 1)
    {
        for (int i = 1; i <= N; i++)
        {
            cout << i << endl;
        }
    }
    else if (N >= 1)
    {
    }
    return 0;
}